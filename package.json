{
  "name": "ntis-public",
  "version": "1.0.0",
  "scripts": {
    "dc:up": "docker-compose -p ntis-public up --build -d --force-recreate",
    "dc:logs": "docker-compose -p ntis-public logs -f",
    "dc:down": "docker-compose -p ntis-public down"
  }
}
